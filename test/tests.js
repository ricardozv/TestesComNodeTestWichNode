const assert = require('assert');

describe('Star Wars Tests', function(){
    it('deve burscar o r2d2 com o formato correto', async () => {
        const expected = [{
            nome: 'R2-D2',
            peso: '96'
        }]
        const nomeBase = `r2-d2`
    })
})